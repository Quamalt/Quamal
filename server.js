function learn_u_p(){
    var user = document.getElementById("user").value;
    var pass = document.getElementById("pass").value;
    // save user and pass to text file
    // append user and pass to text file save text file
    console.log(user, pass);
}
